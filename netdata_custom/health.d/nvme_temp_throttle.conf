template: nvme_temp_high
on: sensors.temp
lookup: temperature
units: °C
every: 60s          # matches NVMe plugin
warn: $this > 70
crit: $this > 80
info: "NVMe temperature high: ${this}°C"
-to: sysadmin
rearm: 5m