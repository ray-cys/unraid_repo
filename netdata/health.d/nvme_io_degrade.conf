template: nvme_io_degrade
on: disk_ops
lookup: ops
units: ops/s
every: 60s          # matches NVMe plugin
warn: $this < (avar(5m, $this) * 0.7)
crit: $this < (avar(5m, $this) * 0.5)
info: "Disk ops reduced compared to baseline"
-to: sysadmin
rearm: 10m